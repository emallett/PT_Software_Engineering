////////*Section 2 Combining Datatypes- Combine objects, arrays, and functions
/*
touch combining_datatypes.js and do your work for this section in this new file
Create an object, called crayonBox, that has a property that is an array. 
The array should have the names of some crayons. Log one of the elements of that array.*/

/* const crayonBox = { names: ['cobalt', 'red', 'canary_yellow', 'coral']}
    console.log=(crayonBox.names[1]); //red */

/* Create an object bottle that has a property that is an object: cap. cap can have properties like material: 'metal' or 'plastic', color: blue or white etc. Log one of the properties of that inner object.

/* const bottleCap= {
    material1: ['metal'],
    material2: ['plastic'],
    color1: ['blue'], 
    color2: ['white'],

const bottle = { cap: { material: 'plastic', 'metal' color: 'blue', 'white' } }
console.log(bottle.cap.color) // blue


3/
/* Create an array called receipt that has at least one object in it. 
The inner objects should be items that have a name and a price. 
Log one of the properties of that inner object.
*/
const receipt = [ { name: 'Beth', price: 5.00 }, { name: 'Merle', price: 9.00 } ]
console.log(receipt[1].name) // Merle

4/
/*Create an array called apartmentBuilding that has an array as one of its elements, 
the inner array should be the names of the tenants. Log one of the elements of the inner array. */


/* const apartmentBuilding = [['Beth', 'Kyle', 'Gray', 'Gill']]
console.log(apartment) 
const apartmentBuilding = [[0][3]]]
console.log(apartmentBuilding[0][3]) // Gill */


//////////////////////////////////
1/
/*
const knit =() => {
        const obj = {
            item: 'scarf',
            size: '6ft'

            return obj.size
        }
     console.log (knit)
     (obj.item); 
     return obj.item */

2/
/*list0CrayonBox {['cobalt', 'red', 'canary_yellow', 'coral']}

    const CrayonSelector = (obj) => {
            for(key in obj){
    
                if(obj[key] === Array) {
                    console.log('I found you')
                }
console.log(crayonBox); */



3//
/*
const powerButton = () => {
    options()
}
powerButton()

const options =() => {
    console.log('select a song')
}
*/
